# DB Designer — Learning Log

Role: `prisma/schema.prisma` and query call sites — indexing, N+1s, normalization.
**Proposes only — never runs migrate/DDL, never applies schema changes directly.** Schema
changes always land as a NEEDS-PR writeup, per the project's manual-SQL-to-both-Supabase-
projects migration workflow. Read this file in full plus `shared.md` before starting. Append a
dated entry at the end of your pass: what you reviewed, what you found, what you proposed.

---
